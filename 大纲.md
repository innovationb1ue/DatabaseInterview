一、 索引（Index）—— 最高频
1. 索引底层数据结构

B+ 树结构特点及优势

B 树与 B+ 树的区别

Hash 索引的特点与适用场景

为什么 MySQL 默认使用 B+ 树而不是红黑树/二叉树/B树？

2. 索引分类与概念

聚簇索引（Clustered Index）与非聚簇索引（二级索引/辅助索引）

主键索引、唯一索引、普通索引、全文索引

覆盖索引（Covering Index）与回表查询

联合索引（复合索引）

3. 索引匹配与匹配原则

最左前缀匹配原则

索引下推（Index Condition Pushdown, ICP）

4. 索引设计与优化

适合建立索引的场景与原则

不适合建立索引的场景

索引失效的常见场景（如函数操作、隐式转换、前导模糊查询等）

前缀索引的优缺点及长度选择

二、 事务与并发控制（Transaction & MVCC）—— 极高频
1. 事务基础

事务的 ACID 特性（原子性、一致性、隔离性、持久性）及实现原理

并发事务带来的问题（脏读、不可重复读、幻读）

2. 事务隔离级别

读未提交（Read Uncommitted）

读已提交（Read Committed）

可重复读（Repeatable Read）—— MySQL 默认隔离级别

串行化（Serializable）

3. 多版本并发控制（MVCC）

MVCC 的概念与解决的问题

隐藏字段（DB_TRX_ID, DB_ROLL_PTR, DB_ROW_ID）的作用

Undo Log 版本链

Read View（读视图）的生成时机与可见性判断规则

RC 和 RR 隔离级别下 MVCC 的差异

MVCC 能否完全解决幻读问题？

三、 锁机制（Locks）—— 极高频
1. 锁的粒度与分类

全局锁、表级锁、行级锁

共享锁（S锁/读锁）与排他锁（X锁/写锁）

意向共享锁（IS）与意向排他锁（IX）的作用

2. 行级锁算法（InnoDB）

记录锁（Record Lock）

间隙锁（Gap Lock）及产生条件

临键锁（Next-Key Lock）及退化规则

3. 常见死锁及排查

死锁产生的原因及四个必要条件

MySQL 如何检测和处理死锁？

如何避免死锁？

死锁排查工具与日志分析（SHOW ENGINE INNODB STATUS）

四、 SQL 优化与执行计划（SQL Optimization）—— 高频
1. 执行计划分析（EXPLAIN）

id 字段的执行顺序

type 字段的连接类型（ALL, index, range, ref, eq_ref, const, system 等）

possible_keys 与 key 字段

key_len 的计算方法

Extra 字段的核心信息（Using filesort, Using temporary, Using index, Using where 等）

2. 常见 SQL 优化场景

大表深度分页（Limit 优化）

JOIN 查询优化（NLJ 算法与 BNL 算法，小表驱动大表）

IN 与 EXISTS 的选择

COUNT(*)、COUNT(1)、COUNT(字段) 的性能对比与原理

ORDER BY 与 GROUP BY 优化（索引排序与文件排序）

3. 慢查询定位

慢查询日志（Slow Query Log）配置与分析

mysqldumpslow 等慢查询分析工具的使用

五、 MySQL 日志系统（Logs）—— 高频
1. 重做日志（Redo Log）

WAL（Write-Ahead Logging）技术

Redo Log 的作用（Crash-safe）与物理记录特性

Redo Log 的写入机制与刷盘策略（innodb_flush_log_at_trx_commit）

2. 回滚日志（Undo Log）

Undo Log 的作用（事务回滚、MVCC）

Insert Undo Log 与 Update Undo Log 的区别

3. 归档日志（Binlog）

Binlog 的作用（主从复制、数据恢复）

Binlog 的三种格式（Statement, Row, Mixed）及优缺点

Redo Log 与 Binlog 的区别

4. 两阶段提交（Two-Phase Commit）

什么是两阶段提交？

为什么需要两阶段提交？解决什么异常场景？

六、 存储引擎（Storage Engines）—— 中高频
1. InnoDB 与 MyISAM 对比

事务支持差异

锁粒度差异

外键支持差异

索引结构与文件存储格式差异（.ibd vs .MYD/.MYI）

COUNT(*) 的性能差异

2. 其他存储引擎

Memory 引擎的特点（内存存储、Hash 索引）

Archive 引擎的特点及场景

3. InnoDB 架构（进阶）

Buffer Pool（缓冲池）的作用与 LRU 淘汰算法优化

Change Buffer（写缓冲）的作用

Log Buffer（日志缓冲）

Doublewrite Buffer（双写缓冲）与页断裂问题

七、 数据库高可用与架构设计（High Availability & Architecture）—— 中高频（偏高级）
1. MySQL 基础架构

连接器、查询缓存、分析器、优化器、执行器

Server 层与存储引擎层的职责划分

一条 SQL 查询语句的执行过程

一条 SQL 更新语句的执行过程

2. 主从复制（Replication）

主从复制的原理与三个线程（Binlog Dump, IO Thread, SQL Thread）

同步、异步与半同步复制（Semi-sync Replication）

主从延迟的原因及排查与解决思路

并行复制原理（MTS）

3. 分库分表（Sharding）

垂直拆分与水平拆分的概念及场景

常见分片算法（Hash, Range 等）

分库分表引入的新问题（分布式事务、跨节点 JOIN、全局主键、排序分页）

常见中间件（ShardingSphere, MyCat 等）

4. 读写分离

读写分离的架构设计

主从延迟导致的“读不到最新数据”问题及解决方案

八、 数据类型与表结构设计（Data Types & Design）—— 中频
1. 常见数据类型

VARCHAR 与 CHAR 的区别与空间占用

DATETIME 与 TIMESTAMP 的区别及 2038 年问题

INT(11) 中 11 的真正含义

FLOAT、DOUBLE 与 DECIMAL 的区别（精度问题）

2. 表设计范式

第一范式（1NF）、第二范式（2NF）、第三范式（3NF）

反范式化设计（Denormalization）及应用场景

逻辑主键与业务主键的选择（为什么推荐自增 ID 或雪花算法 ID 而不是 UUID？）